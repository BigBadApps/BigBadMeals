---
description: BigBad Agent Operating Contract (always apply)
alwaysApply: true
---

# BigBad Agent Operating Contract (AOC)

## Non‑negotiables

- Restate **objective + success criteria** before changes when ambiguous.
- Prefer **small, reviewable diffs**; avoid mega-refactors.
- **No silent behavior changes**: if semantics change, document it.
- **Test changes that matter**: bug fixes + non-trivial logic require tests/updates.
- **Defense in depth**: validate inputs, handle edge cases, fail safely.
- **Security hygiene**: never commit secrets; minimize sensitive data exposure in logs/errors.

## Default workflow

- Understand → Design → Implement → Verify (lint/typecheck/tests) → Deliver (PR summary + test plan) → Operate (logs/metrics/runbook for new critical paths).

## GitHub delivery (this repo)

- Ship changes through **PRs to `main`**; follow `AGENTS.md` and `docs/coding-standards/standards/DELIVERY.md`.
- When you **create or update a PR**, always **push to Git**, then **wait for GitHub** (CI + merge), then **do the recommended next step** yourself (see root `AGENTS.md` → “Mandatory procedure for coding agents”).
- Preferred one-shot: `npm run pr:publish:wait` (or `bash scripts/pr-publish.sh --wait`) with a long enough execution budget for CI.
- To read PR state once: `npm run pr:status -- <n>` or `gh pr view <n> --json state,mergedAt,statusCheckRollup,autoMergeRequest,url`. There is no async push into the chat—**poll** until merged, failed, or timeout.

